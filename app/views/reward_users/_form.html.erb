<%= form_with(model: reward_user) do |form| %>
  <% if reward_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reward_user.errors.count, "error") %> prohibited this reward_user from being saved:</h2>

      <ul>
        <% reward_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
