<%= form_with(model: transaction_user) do |form| %>
  <% if transaction_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transaction_user.errors.count, "error") %> prohibited this transaction_user from being saved:</h2>

      <ul>
        <% transaction_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :point %>
    <%= form.text_field :point %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
